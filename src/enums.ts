export enum Currencies {
  BTC = 'btc',
  STX = 'stx',
  USD = 'usd',
}

export enum Breakpoints {
  sm = 576,
  md = 768,
  lg = 992,
  xl = 1200,
}

export enum ButtonColors {
  red = 'red',
  black = 'black',
}
